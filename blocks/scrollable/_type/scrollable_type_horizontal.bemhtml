block('scrollable').mod('type', 'horizontal')(

    content()(function () {
        return [
            applyNext(),
            {
                block: 'scroll',
                mods: {
                    type: 'horizontal'
                }
            }
        ];
    })
)
